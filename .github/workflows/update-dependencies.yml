name: Update Dependencies
on:
  workflow_dispatch:
  schedule:
    - cron: 0 21 * * 5

permissions:
  contents: write
  pull-requests: write

jobs:
  update-dependencies:
    if: ${{ github.repository == 'alextheman231/music' }}
    uses: alextheman231/github-actions/.github/workflows/update-dependencies.yml@c1bd7268c2c089e5f12acfdc187b4d23e0259f47 # v12.9.1
    secrets:
      APP_ID: ${{ vars.ALEX_UP_BOT_APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.ALEX_UP_BOT_PRIVATE_KEY }}
    with:
      update_actions: true
      body-path: .github/pull_request_template.md
